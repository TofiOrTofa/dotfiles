floating_modifier $mod

bindsym --locked $button_sun_up exec $sun_up
bindsym --locked $button_sun_down exec $sun_down
bindsym --locked $button_sun_max exec $sun_max
bindsym --locked $button_sun_min exec $sun_min

bindsym --locked $button_pause_media exec $media_pause
bindsym --locked $button_play_media exec $media_play
bindsym --locked $button_next_media exec $media_next
bindsym --locked $button_prev_media exec $media_prev

bindsym --locked $button_vol_up exec $vol_up
bindsym --locked $button_vol_down exec $vol_down
bindsym --locked $button_vol_max exec $vol_max
bindsym --locked $button_vol_min exec $vol_min
bindsym --locked $button_vol_mute exec $vol_mute

bindsym --locked $button_mic_up exec $mic_up
bindsym --locked $button_mic_down exec $mic_down
bindsym --locked $button_mic_max exec $mic_max
bindsym --locked $button_mic_min exec $mic_min
bindsym --locked $button_mic_mute exec $mic_mute
bindsym --to-code $button_screenshot exec $screenshot_app

bindsym --to-code Shift+semicolon exec ~/.config/wofi/scripts/commands.lua, mode "insert"

bindsym --to-code Shift+c reload
bindsym --to-code Shift+r restart
bindsym --to-code Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Waylend session.' -B 'Yes, exit sway' 'swaymsg exit'
bindsym --to-code Shift+p exec output eDP-1 toggle

#bindsy --to-codem Shift+p exec $sun_mute
bindsym --to-code t exec $terminal

bindsym --to-code Shift+q kill

bindsym --to-code Shift+Escape exec swaymsg "output eDP-1 power toggle" 

bindsym --to-code q move scratchpad
bindsym --to-code m scratchpad show

bindsym --to-code g gaps inner current toggle 10; gaps outer current toggle 5
bindsym --to-code Shift+g border toggle

bindsym --to-code f floating toggle
bindsym --to-code Shift+f fullscreen toggle

bindsym --to-code s layout stacking
bindsym --to-code w layout tabbed
bindsym --to-code e layout toggle split
bindsym --to-code Shift+t layout toggle tabbed

bindsym --to-code h focus left
bindsym --to-code j focus down
bindsym --to-code k focus up
bindsym --to-code l focus right

bindsym --to-code period exec swaymsg workspace number $(($(swaymsg -t get_workspaces | jq '.[] | select(.focused==true).num') + 1))
bindsym --to-code comma exec swaymsg workspace number $(($(swaymsg -t get_workspaces | jq '.[] | select(.focused==true).num') - 1))
bindsym --to-code $alt+period workspace next
bindsym --to-code $alt+comma workspace prev

bindsym --to-code p focus parent
bindsym --to-code c focus child

bindsym --to-code Shift+h move left
bindsym --to-code Shift+j move down
bindsym --to-code Shift+k move up
bindsym --to-code Shift+l move right

# Перем --to-codeестить в следующий (+1)
bindsym --to-code Shift+period exec swaymsg move container to workspace number $(($(swaymsg -t get_workspaces | jq '.[] | select(.focused==true).num') + 1))
bindsym --to-code Shift+comma exec swaymsg move container to workspace number $(($(swaymsg -t get_workspaces | jq '.[] | select(.focused==true).num') - 1))

bindsym --to-code $alt+Shift+period move container to workspace next
bindsym --to-code $alt+Shift+comma move container to workspace prev

bindsym --to-code $ctrl+k exec $vol_up
bindsym --to-code $ctrl+j exec $vol_down
bindsym --to-code $ctrl+h exec $media_prev
bindsym --to-code $ctrl+l exec $media_next

bindsym $ctrl+Up exec $vol_up
bindsym $ctrl+Down exec $vol_down

bindsym $ctrl+space exec $media_play
bindsym $ctrl+Left exec $media_prev
bindsym $ctrl+Right exec $media_next

bindsym 1 $ws1
bindsym 2 $ws2
bindsym 3 $ws3
bindsym 4 $ws4
bindsym 5 $ws5
bindsym 6 $ws6
bindsym 7 $ws7
bindsym 8 $ws8
bindsym 9 $ws9
bindsym 0 $ws10
bindsym $alt+1 $ws11
bindsym $alt+2 $ws12
bindsym $alt+3 $ws13
bindsym $alt+4 $ws14
bindsym $alt+5 $ws15
bindsym $alt+6 $ws16
bindsym $alt+7 $ws17
bindsym $alt+8 $ws18
bindsym $alt+9 $ws19
bindsym $alt+0 $ws20

bindsym Shift+1 move container to $ws1
bindsym Shift+2 move container to $ws2
bindsym Shift+3 move container to $ws3
bindsym Shift+4 move container to $ws4
bindsym Shift+5 move container to $ws5
bindsym Shift+6 move container to $ws6
bindsym Shift+7 move container to $ws7
bindsym Shift+8 move container to $ws8
bindsym Shift+9 move container to $ws9
bindsym Shift+0 move container to $ws10
bindsym $alt+Shift+1 move container to $ws11
bindsym $alt+Shift+2 move container to $ws12
bindsym $alt+Shift+3 move container to $ws13
bindsym $alt+Shift+4 move container to $ws14
bindsym $alt+Shift+5 move container to $ws15
bindsym $alt+Shift+6 move container to $ws16
bindsym $alt+Shift+7 move container to $ws17
bindsym $alt+Shift+8 move container to $ws18
bindsym $alt+Shift+9 move container to $ws19
bindsym $alt+Shift+0 move container to $ws20

bindsym --to-code r mode "resize"
bindsym --to-code o mode "insert"
bindsym --to-code a mode "insert"
bindsym --to-code i mode "insert"
bindsym --to-code d mode "wofi"
bindsym --to-code v mode "micro"

mode "insert" {
  bindsym --locked $button_sun_up exec $sun_up
  bindsym --locked $button_sun_down exec $sun_down
  bindsym --locked $button_sun_max exec $sun_max
  bindsym --locked $button_sun_min exec $sun_min
  
  bindsym --locked $button_pause_media exec $media_pause
  bindsym --locked $button_play_media exec $media_play
  bindsym --locked $button_next_media exec $media_next
  bindsym --locked $button_prev_media exec $media_prev
  
  bindsym --locked $button_vol_up exec $vol_up
  bindsym --locked $button_vol_down exec $vol_down
  bindsym --locked $button_vol_max exec $vol_max
  bindsym --locked $button_vol_min exec $vol_min
  bindsym --locked $button_vol_mute exec $vol_mute
  
  bindsym --locked $button_mic_up exec $mic_up
  bindsym --locked $button_mic_down exec $mic_down
  bindsym --locked $button_mic_max exec $mic_max
  bindsym --locked $button_mic_min exec $mic_min
  bindsym --locked $button_mic_mute exec $mic_mute

  bindsym $button_screenshot exec $screenshot_app

  bindsym Super_L mode "default"
  bindsym Alt_R mode "default"
}
mode "old_insert" {
  bindsym --locked $button_sun_up exec $sun_up
  bindsym --locked $button_sun_down exec $sun_down
  bindsym --locked $button_sun_max exec $sun_max
  bindsym --locked $button_sun_min exec $sun_min
  
  bindsym --locked $button_pause_media exec $media_pause
  bindsym --locked $button_play_media exec $media_play
  bindsym --locked $button_next_media exec $media_next
  bindsym --locked $button_prev_media exec $media_prev
  
  bindsym --locked $button_vol_up exec $vol_up
  bindsym --locked $button_vol_down exec $vol_down
  bindsym --locked $button_vol_max exec $vol_max
  bindsym --locked $button_vol_min exec $vol_min
  bindsym --locked $button_vol_mute exec $vol_mute
  
  bindsym --locked $button_mic_up exec $mic_up
  bindsym --locked $button_mic_down exec $mic_down
  bindsym --locked $button_mic_max exec $mic_max
  bindsym --locked $button_mic_min exec $mic_min
  bindsym --locked $button_mic_mute exec $mic_mute

  bindsym $button_screenshot exec $screenshot_app

  bindsym Super_L+c mode "default"
  bindsym Alt_R mode "default"
}
mode "resize" {
  bindsym --locked $button_sun_up exec $sun_up
  bindsym --locked $button_sun_down exec $sun_down
  bindsym --locked $button_sun_max exec $sun_max
  bindsym --locked $button_sun_min exec $sun_min
 
  bindsym --locked $button_pause_media exec $media_pause
  bindsym --locked $button_play_media exec $media_play
  bindsym --locked $button_next_media exec $media_next
  bindsym --locked $button_prev_media exec $media_prev
  
  bindsym --locked $button_vol_up exec $vol_up
  bindsym --locked $button_vol_down exec $vol_down
  bindsym --locked $button_vol_max exec $vol_max
  bindsym --locked $button_vol_min exec $vol_min
  bindsym --locked $button_vol_mute exec $vol_mute
  
  bindsym --locked $button_mic_up exec $mic_up
  bindsym --locked $button_mic_down exec $mic_down
  bindsym --locked $button_mic_max exec $mic_max
  bindsym --locked $button_mic_min exec $mic_min
  bindsym --locked $button_mic_mute exec $mic_mute

  bindsym $button_screenshot exec $screenshot_app

  bindsym h resize shrink width 1 px or 1 ppt
  bindsym j resize grow height 1 px or 1 ppt
  bindsym k resize shrink height 1 px or 1 ppt
  bindsym l resize grow width 1 px or 1 ppt

  bindsym Shift+h resize shrink width 10 px or 5 ppt
  bindsym Shift+j resize grow height 10 px or 5 ppt
  bindsym Shift+k resize shrink height 10 px or 5 ppt
  bindsym Shift+l resize grow width 10 px or 5 ppt

  bindsym $ctrl+h resize shrink width 100 px or 10 ppt
  bindsym $ctrl+j resize grow height 100 px or 10 ppt
  bindsym $ctrl+k resize shrink height 100 px or 10 ppt
  bindsym $ctrl+l resize grow width 100 px or 10 ppt

  bindsym $ctrl+Shift+h resize shrink width 400 px or 20 ppt
  bindsym $ctrl+Shift+j resize grow height 400 px or 20 ppt
  bindsym $ctrl+Shift+k resize shrink height 400 px or 20 ppt
  bindsym $ctrl+Shift+l resize grow width 400 px or 20 ppt

  bindsym Left resize shrink width 1 px or 1 ppt
  bindsym Down resize grow height 1 px or 1 ppt
  bindsym Up resize shrink height 1 px or 1 ppt
  bindsym Right resize grow width 1 px or 1 ppt

  bindsym Shift+Left resize shrink width 10 px or 5 ppt
  bindsym Shift+Down resize grow height 10 px or 5 ppt
  bindsym Shift+Up resize shrink height 10 px or 5 ppt
  bindsym Shift+Right resize grow width 10 px or 5 ppt

  bindsym $ctrl+Left resize shrink width 100 px or 10 ppt
  bindsym $ctrl+Down resize grow height 100 px or 10 ppt
  bindsym $ctrl+Up resize shrink height 100 px or 10 ppt
  bindsym $ctrl+Right resize grow width 100 px or 10 ppt

  bindsym $ctrl+Shift+Left resize shrink width 400 px or 20 ppt
  bindsym $ctrl+Shift+Down resize grow height 400 px or 20 ppt
  bindsym $ctrl+Shift+Up resize shrink height 400 px or 20 ppt
  bindsym $ctrl+Shift+Right resize grow width 400 px or 20 ppt

  bindsym Super_L mode "default"
  bindsym Alt_R mode "default"
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym o mode "insert"
  bindsym a mode "insert"
  bindsym v mode "micro"
  bindsym m mode "micro"
  bindsym d mode "wofi"
}
mode "micro" {
  bindsym --locked $button_sun_up exec $sun_up
  bindsym --locked $button_sun_down exec $sun_down
  bindsym --locked $button_sun_max exec $sun_max
  bindsym --locked $button_sun_min exec $sun_min
 
  bindsym --locked $button_pause_media exec $media_pause
  bindsym --locked $button_play_media exec $media_play
  bindsym --locked $button_next_media exec $media_next
  bindsym --locked $button_prev_media exec $media_prev
  
  bindsym --locked $button_vol_up exec $vol_up
  bindsym --locked $button_vol_down exec $vol_down
  bindsym --locked $button_vol_max exec $vol_max
  bindsym --locked $button_vol_min exec $vol_min
  bindsym --locked $button_vol_mute exec $vol_mute
  
  bindsym --locked $button_mic_up exec $mic_up
  bindsym --locked $button_mic_down exec $mic_down
  bindsym --locked $button_mic_max exec $mic_max
  bindsym --locked $button_mic_min exec $mic_min
  bindsym --locked $button_mic_mute exec $mic_mute

  bindsym $button_screenshot exec $screenshot_app

  bindsym p exec $vol_app

  bindsym $ctrl+Up exec $mic_up
  bindsym $ctrl+Down exec $mic_down
  bindsym j exec $mic_down
  bindsym k exec $mic_up

  bindsym Super_L mode "default"
  bindsym Alt_R mode "default"
  bindsym o mode "insert"
  bindsym a mode "insert"
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym d mode "wofi"
}
mode "wofi" {
  bindsym --locked $button_sun_up exec $sun_up
  bindsym --locked $button_sun_down exec $sun_down
  bindsym --locked $button_sun_max exec $sun_max
  bindsym --locked $button_sun_min exec $sun_min
 
  bindsym --locked $button_pause_media exec $media_pause
  bindsym --locked $button_play_media exec $media_play
  bindsym --locked $button_next_media exec $media_next
  bindsym --locked $button_prev_media exec $media_prev
  
  bindsym --locked $button_vol_up exec $vol_up
  bindsym --locked $button_vol_down exec $vol_down
  bindsym --locked $button_vol_max exec $vol_max
  bindsym --locked $button_vol_min exec $vol_min
  bindsym --locked $button_vol_mute exec $vol_mute
  
  bindsym --locked $button_mic_up exec $mic_up
  bindsym --locked $button_mic_down exec $mic_down
  bindsym --locked $button_mic_max exec $mic_max
  bindsym --locked $button_mic_min exec $mic_min
  bindsym --locked $button_mic_mute exec $mic_mute

  bindsym $button_screenshot exec $screenshot_app

  bindsym --to-code d exec $search_apps -show drun, mode "insert"
  bindsym --to-code f exec $search_apps -show filebrowser, mode "insert"
  bindsym --to-code r exec $search_apps -show recursivebrowser, mode "insert"
  bindsym --to-code w exec .config/wofi/scripts/windows.py, mode "insert"

  bindsym Super_L mode "default"
  bindsym Alt_R mode "default"
  bindsym Escape mode "default"
  bindsym Return mode "default"
  bindsym o mode "insert"
  bindsym a mode "insert"
}

